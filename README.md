# MySQL

__Connecting to the server__

If the SQL Server is running on other machine:
```
mysql -h [host] -u [user] -p
```

If the SQL Server is running on current machine then you can omit the parameter "host (-h)":
```
mysql -u [user] -p
```

